version: '2'
services:
  builder:
    image: amarkwalder/amarkwalder.github.io-builder
    build:
      context: .
      dockerfile: Dockerfile.build
    environment:
      - BUILD_NO
    volumes:
      - ./builds:/builds
